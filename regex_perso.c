#include "regex_perso.h"

/**
 * union : +
 * concaténation : ab (a et b sont concaténés)
*/

void pass_to_graphe_aux(graphe_t* graphe, char exp){

}

graphe_t pass_to_tree(char* exp){

}

graphe_t pass_to_graphe(char* exp){

    if(exp == NULL){
        return NULL;
    }
    return init_graphe();
}

int main(void){
    return EXIT_SUCCESS;
}