#include "graphe.h"

#ifndef REGEX_PERSO

graphe_t pass_to_tree(char* exp);
graphe_t pass_to_graphe(char* exp);
graphe_t pass_to_exp(graphe_t graphe);

int valide_word(graphe_t graphe, char* word);

#endif